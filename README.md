# Book Pass

Pass notes like school. Typing is disabled.

Draw a note, fold it, and pass its link. The drawing is compressed into the URL fragment, so it is never uploaded or stored.

```sh
npm install
npm run dev
```

Built by [72F Studio](https://72fstudio.in).
